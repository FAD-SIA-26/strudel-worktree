# Changesets

Use `pnpm changeset` to describe the version bump for `worktree-orchestrator`.

- `patch`: fixes and small improvements
- `minor`: new backward-compatible features
- `major`: breaking changes

Merge to `main` updates the release PR automatically. PRs also get preview tarballs with a prerelease version suffix.
